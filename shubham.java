
// import java.util.*;
import java.util.Scanner;

public class shubham {
    public static void main(String[] args) {
        // cin<<
        Scanner scn = new Scanner(System.in);
        int num = scn.nextInt();
        double num1 = scn.nextDouble();
        long num2 = scn.nextLong();
        String str = scn.next();

        // Outputs
        // cout<<""<<endl
        System.out.println("Outputs are=============================");
        System.out.println(num);
        System.out.println(num1);
        System.out.println(num2);
        System.out.println(str);

    }

    // ab name_=new ab(10,20);
    // Ye basic codes

    // 1)syntax clear
    // 2)familiarity(process,functions,oops)

    // even or odd
    // if(num%2==0) even else odd

    // prime or not
    // loop lagayenge 2 se num-1 aur condition if(num%loop ka i==0) return false;
    // return true;

    // sum of n natural numbers

    // print kr do (n*(n+1))/2;

    // gcd(HCF)
    
    // LCM
    // a and b
    // LCM=(a*b)/gcd(a,b);

    // reverse a number

        
}